x = 1

def simple_loop():
    y = 0
    for x in (1, 2, 3):
        y = y + x
    return y


def range_loop():
    y = 0
    for x in range(0, 10):
        y = y + x

    for x in range(10):
        y = y + x

    return y

assert simple_loop() == 6
assert range_loop() == 90

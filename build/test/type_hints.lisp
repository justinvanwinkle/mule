
(EVAL-WHEN (:COMPILE-TOPLEVEL :LOAD-TOPLEVEL :EXECUTE)
  (UNLESS (FIND-PACKAGE "type_hints")
    (MAKE-PACKAGE "type_hints" :USE '("COMMON-LISP"))))
(IN-PACKAGE "type_hints")
(IF (NOT (EQUAL (PACKAGE-NAME *PACKAGE*) "builtins"))
    (LOAD "/home/jvanwink/repos/mule/build/lib/builtins.fasl"))
(USE-PACKAGE "builtins")
(DEFUN |foo| ()
  (LET ((|x| 0))
    (DECLARE (TYPE FIXNUM |x|))
    (LET ((|lim| 4000))
      (LOOP WHILE (< (THE FIXNUM |x|) |lim|)
            DO (SETF |x| (THE FIXNUM (+ |x| 1))))
      (RETURN-FROM |foo| |x|))))
(ASSERT (EQUALP (|foo|) 4000))
(LOOP FOR S BEING EACH PRESENT-SYMBOL IN *PACKAGE*
      WHEN (OR (FBOUNDP S) (BOUNDP S) (FIND-CLASS S NIL))
      DO (EXPORT S))
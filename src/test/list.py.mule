
x = []
y = [9]
z = [1, 2, 3]

def foo():
    l = []
    x = 0
    for e in l:
        x = 1
    return x

def bar():
    l = [1, 2, 3]
    x = 0
    for e in l:
        x = x + e
    return x

assert z[0] == 1
assert y[0] == 9
assert foo() == 0
assert bar() == 6

z[1] = "hey"
assert z == [1, "hey", 3]
